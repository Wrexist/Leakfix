export function SiteFooter() {
  return (
    <footer className="border-t border-line">
      <div className="mx-auto flex w-full max-w-6xl flex-col gap-2 px-5 py-8 text-sm text-ink-faint sm:flex-row sm:items-center sm:justify-between sm:px-8">
        <p>LeakFix</p>
        <p>Read-only scans, limited to public web pages.</p>
      </div>
    </footer>
  );
}
